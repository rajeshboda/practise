from utilties import  is_positive
from utilties import get_me_fib
from utilties import get_me_even
# the above import just tell this module that we can use the methods, but we have to use utilities


print(get_me_even("module", 1))
print(get_me_fib("module", 1000))
print(is_positive("module", -1))
