import  utilties as utilv1
# the above import just tell this module that we can use the methods, but we have to use utilities
# here we can give alias just avoid the module name


print(utilv1.get_me_even("module", 1))
print(utilv1.get_me_fib("module", 1000))
print(utilv1.is_positive("module", -1))
